name: Tests

on:
  push:
    tags:
    branches:
  pull_request:

jobs:

  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go 1.22
      uses: actions/setup-go@v5
      with:
        go-version: "1.22"
    - name: Test
      run: |
        make test
